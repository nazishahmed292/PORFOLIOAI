# Base URL of the API as seen by the browser. Keep "/api" so the Vite dev proxy
# (and nginx in production) forward requests to the backend.
VITE_API_BASE_URL=/api

# Dev-server only: where the Vite proxy sends /api requests.
VITE_DEV_PROXY_TARGET=http://localhost:8000
