[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    postgres: needs a real PostgreSQL server with pgvector (skipped when none is reachable; set REQUIRE_POSTGRES=1 to fail instead)
